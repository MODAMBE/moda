services:
  - type: web
    name: modapp-eglise
    env: python
    buildCommand: "pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
    startCommand: "gunicorn ModApp.wsgi:application"
    plan: free
